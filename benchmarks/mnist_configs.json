{
  "training_presets": {
    "ps_1w_1s_base_barrier": {
      "workers": 1, "servers": 1,
      "algorithm": "parameter_server",
      "serializer": "base", "sync": "barrier", "store": "blocking"
    },
    "ps_2w_1s_base_barrier": {
      "workers": 2, "servers": 1,
      "algorithm": "parameter_server",
      "serializer": "base", "sync": "barrier", "store": "blocking"
    },
    "ps_3w_1s_base_barrier": {
      "workers": 3, "servers": 1,
      "algorithm": "parameter_server",
      "serializer": "base", "sync": "barrier", "store": "blocking"
    },
    "ps_3w_2s_base_barrier": {
      "workers": 3, "servers": 2,
      "algorithm": "parameter_server",
      "serializer": "base", "sync": "barrier", "store": "blocking"
    },
    "ps_2w_1s_sparse_barrier": {
      "workers": 2, "servers": 1,
      "algorithm": "parameter_server",
      "serializer": "sparse", "sync": "barrier", "store": "blocking"
    },
    "ps_2w_1s_nonblocking": {
      "workers": 2, "servers": 1,
      "algorithm": "parameter_server",
      "serializer": "base", "sync": "nonblocking", "store": "wild"
    },
    "ar_2w_base": {
      "workers": 2, "servers": 0,
      "algorithm": "all_reduce",
      "serializer": "base", "sync": null, "store": null
    },
    "ar_3w_base": {
      "workers": 3, "servers": 0,
      "algorithm": "all_reduce",
      "serializer": "base", "sync": null, "store": null
    },
    "ar_2w_sparse": {
      "workers": 2, "servers": 0,
      "algorithm": "all_reduce",
      "serializer": "sparse", "sync": null, "store": null
    }
  },
  "profiles": {
    "smoke": {
      "train_samples": null,
      "test_samples": null,
      "models": ["dense_small", "conv_small"],
      "training_presets": ["ar_3w_base", "ps_2w_1s_base_barrier"],
      "defaults": {
        "max_epochs": 100,
        "batch_size": 256,
        "lr": 0.5,
        "offline_epochs": 0,
        "early_stopping_tolerance": 1e-4,
        "min_accuracy": 0.85,
        "max_train_seconds": 600
      },
      "overrides": {
        "ar_3w_base": { "lr": 1.0 },
        "conv_small":  { "max_epochs": 150, "max_train_seconds": 900 }
      }
    },
    "benchmark": {
      "train_samples": null,
      "test_samples": null,
      "models": ["dense_small", "dense_large", "conv_tiny", "conv_small", "conv_large"],
      "training_presets": [
        "ps_2w_1s_base_barrier",
        "ps_2w_1s_nonblocking",
        "ps_2w_1s_sparse_barrier",
        "ar_2w_base",
        "ar_2w_sparse"
      ],
      "defaults": {
        "max_epochs": 150,
        "batch_size": 256,
        "lr": 0.5,
        "offline_epochs": 0,
        "early_stopping_tolerance": 1e-4,
        "min_accuracy": 0.85,
        "max_train_seconds": 900
      },
      "overrides": {
        "ar_2w_base":   { "lr": 1.0 },
        "ar_2w_sparse": { "lr": 1.0 },
        "conv_tiny":    { "min_accuracy": 0.80, "max_epochs": 200, "max_train_seconds": 1200 },
        "conv_small":   { "max_epochs": 200, "max_train_seconds": 1200 },
        "conv_large":   { "max_epochs": 200, "max_train_seconds": 1500 },
        "dense_large":  { "max_epochs": 120 }
      }
    }
  }
}
