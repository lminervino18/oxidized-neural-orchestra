# Parameter Server

## Env

A `.env` file must be created in this exact directory with the following variables (with some example values).

```bash
HOST=0.0.0.0  # The host to bind the tcp listener to.
PORT=8765     # The port to use.
RUST_LOG=info # The log level for the server.
```
