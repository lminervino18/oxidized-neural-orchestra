# docker
.dockerignore
compose.yaml

# Git
.git/
.github/
.gitignore

# Project
README.md
assets/
docker/
docs/
notebooks/
target

# Heavy non-Rust paths the node image never needs (keep the build context small)
.venv
**/.venv
doc/
data/
tmp/
*.safetensors
