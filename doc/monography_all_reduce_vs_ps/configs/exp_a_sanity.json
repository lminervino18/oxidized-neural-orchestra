{
  "name": "exp_a_sanity",
  "_doc": "Exp A — sanity/correctness on MNIST/nielsen with the reference recipe (batch 10 per worker, lr 0.1). Validates that every strategy trains correctly and reaches the known MNIST ceiling. REDESIGN: PS now runs 2 SHARDED servers with the SAME worker count as AR (equal-workers fairness); the single-server strawman is gone. strategy_switch dropped (never switches — known bug).",
  "runs": [
    {"label": "A_baseline_mnist", "dataset": "mnist", "model": "nielsen", "strategy": "baseline",
     "workers": 1, "servers": 0, "batch_size": 10, "max_epochs": 30, "offline_epochs": 0,
     "lr": 0.1, "loss_fn": "cross_entropy", "seed": 42, "eval": true,
     "notes": "single-process PyTorch reference"},

    {"label": "A_ar_mnist", "dataset": "mnist", "model": "nielsen", "strategy": "all_reduce",
     "workers": 3, "servers": 0, "batch_size": 10, "max_epochs": 30, "offline_epochs": 0,
     "lr": 0.1, "loss_fn": "cross_entropy", "seed": 42, "eval": true,
     "notes": "all-reduce, 3 workers"},

    {"label": "A_ps_mnist", "dataset": "mnist", "model": "nielsen", "strategy": "parameter_server",
     "workers": 3, "servers": 2, "batch_size": 10, "max_epochs": 30, "offline_epochs": 0,
     "lr": 0.1, "loss_fn": "cross_entropy", "seed": 42, "eval": true,
     "notes": "PS barrier + blocking store, 3 workers + 2 sharded servers"}
  ]
}
